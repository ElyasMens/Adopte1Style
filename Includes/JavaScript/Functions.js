/*document.querySelector("#input1").addEventListener('input', function() {
    var inputValue = this.value.trim(); // Récupérer la valeur de l'input et supprimer les espaces vides
    
    if (inputValue === '') {
        // Ajouter une classe d'erreur si l'input est vide
        this.classList.add('error-border');
    } else {
        // Retirer la classe d'erreur si l'input n'est pas vide
        this.classList.remove('error-border');
    }
});

*/